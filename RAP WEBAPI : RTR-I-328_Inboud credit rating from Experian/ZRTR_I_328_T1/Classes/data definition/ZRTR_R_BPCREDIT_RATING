@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Root View for BP Credit Rating'
@Metadata.ignorePropagatedAnnotations: true
define root view entity ZRTR_R_BPCREDIT_RATING as select from ZRTR_R_RATING
{
    
    key Partner,
    key Timestamp,
    key Diftval,
    key DateTo,
    key GradeMethod,
    DateFrom,
    FlgPermit,
    Grade,
    Tendency,
    DateWhen,
    FlgDefault,
    Text
}
